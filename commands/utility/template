// Slash command names must be between 1-32 characters and contain
// no capital letters, spaces, or symbols other than - and _

const { SlashCommandBuilder } = require('discord.js');

module.exports = {
	data: new SlashCommandBuilder()
		.setName('')
		.setDescription(''),
	async execute(interaction) {
    // logic
  },
};
